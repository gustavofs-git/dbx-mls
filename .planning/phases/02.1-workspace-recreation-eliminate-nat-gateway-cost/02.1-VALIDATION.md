---
phase: 02.1
slug: workspace-recreation-eliminate-nat-gateway-cost
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-14
---

# Phase 02.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Manual CLI verification (no automated test suite — pure infra phase) |
| **Config file** | none |
| **Quick run command** | `az databricks workspace show --name <workspace> --resource-group <rg> --query properties.provisioningState` |
| **Full suite command** | `databricks bundle validate --target dev` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run quick verification command where applicable
- **After every plan wave:** Run `databricks bundle validate --target dev`
- **Before `/gsd:verify-work`:** Full smoke test job must pass
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| deletion | 01 | 1 | D-01/D-02 | manual | `az group show --name <workspace-rg>` exits non-zero | N/A | ⬜ pending |
| workspace-create | 01 | 2 | D-03/D-04 | manual | `az databricks workspace show` → provisioningState=Succeeded | N/A | ⬜ pending |
| sp-admin | 01 | 3 | D-09 | manual | Workspace UI shows SP in admins group | N/A | ⬜ pending |
| github-host-update | 01 | 3 | D-11 | manual | `gh variable list` shows updated DATABRICKS_HOST | N/A | ⬜ pending |
| secret-scope | 01 | 4 | D-16 | manual | `databricks secrets list-secrets lol-pipeline` shows riot-api-key | N/A | ⬜ pending |
| bundle-deploy | 01 | 4 | D-15 | CLI | `databricks bundle validate --target dev` exits 0 | ✅ | ⬜ pending |
| smoke-test | 01 | 5 | D-15 | manual | GitHub Actions cd-dev.yml run succeeds | N/A | ⬜ pending |
| article | 02 | 1 | D-20/D-21 | file | `docs/posts/phase-2.1-article.md` exists with content | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- No test framework installation needed — this is a pure infrastructure phase
- Azure CLI installation may be required on executor machine (research flagged this)

*Existing infrastructure: No automated tests applicable for cloud infrastructure provisioning tasks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Workspace deleted (no NAT gateway remains) | D-01/D-02 | Azure resource deletion cannot be automated in CI | `az group show --name <workspace-rg>` must return 404 |
| New workspace has no NAT gateway | D-04 | Requires Azure Portal / CLI inspection of managed RG | `az network nat-gateway list --resource-group <managed-rg>` must return empty |
| SP is workspace admin | D-09 | No CLI command to verify workspace-level admin role | Databricks UI → Settings → Identity and Access → Admins shows SP |
| Bundle deploys successfully | D-15 | Requires valid workspace + correct DATABRICKS_HOST | `databricks bundle deploy --target dev` exits 0, then GitHub Action green |
| Riot API key accessible from cluster | D-16 | Requires running cluster | `databricks secrets list-secrets lol-pipeline` shows `riot-api-key` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
