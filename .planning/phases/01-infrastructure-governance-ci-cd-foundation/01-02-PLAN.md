---
phase: 01-infrastructure-governance-ci-cd-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/cd-dev.yml
  - .github/workflows/cd-prod.yml
  - docs/setup.md
autonomous: false
requirements:
  - INFRA-01
  - CICD-01
  - CICD-02
  - CICD-03
  - CICD-04
  - CICD-05
  - CICD-06

must_haves:
  truths:
    - "A push to `main` triggers `ci.yml` (validate + pytest) and `cd-dev.yml` (deploy) — both complete with green checkmarks in GitHub Actions"
    - "Zero PAT tokens exist in GitHub Secrets (only repository variables `DATABRICKS_HOST` and `DATABRICKS_CLIENT_ID`)"
    - "`ci.yml` runs on every push to any branch, including feature branches"
    - "`cd-prod.yml` only deploys on `v*` tag pushes, uses `environment: prod`, and has concurrency group `prod-deploy` with `cancel-in-progress: false`"
    - "All three workflows authenticate via OIDC — no `DATABRICKS_TOKEN` environment variable appears anywhere"
    - "Databricks CLI is pinned to `v0.295.0` in `cd-prod.yml` and to `@v0.295.0` in `ci.yml` and `cd-dev.yml`"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Validate + pytest on every push"
      contains: "id-token: write"
    - path: ".github/workflows/cd-dev.yml"
      provides: "Dev deploy + smoke test trigger on push to main"
      contains: "databricks bundle deploy --target dev"
    - path: ".github/workflows/cd-prod.yml"
      provides: "Prod deploy on v* tags with manual approval gate"
      contains: "environment: prod"
  key_links:
    - from: ".github/workflows/cd-prod.yml"
      to: "GitHub Environment protection rule (prod)"
      via: "environment: prod job key — must exactly match federation policy subject `repo:<org>/dbx-mls:environment:prod`"
      pattern: "environment: prod"
    - from: ".github/workflows/ci.yml"
      to: "Databricks workspace"
      via: "OIDC token exchange via DATABRICKS_AUTH_TYPE: github-oidc"
      pattern: "DATABRICKS_AUTH_TYPE"
    - from: ".github/workflows/cd-dev.yml"
      to: "smoke_test_job"
      via: "databricks bundle run smoke_test_job --target dev after deploy"
      pattern: "bundle run smoke_test_job"

user_setup:
  - service: "GitHub Repository Variables"
    why: "OIDC workflows reference DATABRICKS_HOST and DATABRICKS_CLIENT_ID as repository variables (not secrets)"
    dashboard_config:
      - task: "Create repository variable DATABRICKS_HOST"
        location: "GitHub repo → Settings → Secrets and variables → Actions → Variables tab → New repository variable"
        value_format: "https://adb-<workspace-id>.<region>.azuredatabricks.net"
      - task: "Create repository variable DATABRICKS_CLIENT_ID"
        location: "GitHub repo → Settings → Secrets and variables → Actions → Variables tab → New repository variable"
        value_format: "UUID of the Service Principal application ID"
  - service: "GitHub Environment (prod)"
    why: "cd-prod.yml job uses `environment: prod` which triggers GitHub Environment protection rules (manual approval gate)"
    dashboard_config:
      - task: "Create GitHub Environment named exactly `prod` (lowercase)"
        location: "GitHub repo → Settings → Environments → New environment"
      - task: "Add yourself as Required reviewer on the `prod` environment"
        location: "GitHub repo → Settings → Environments → prod → Required reviewers"
  - service: "Databricks OIDC Federation Policies"
    why: "The Service Principal must have two federation policies: one branch-scoped (dev) and one environment-scoped (prod). These are created via Databricks CLI against the account console, not the workspace."
    env_vars:
      - name: DATABRICKS_ACCOUNT_ID
        source: "Databricks Account Console → top-right user menu → Account ID"
    dashboard_config:
      - task: "Authenticate CLI to Databricks account console"
        location: "Run: databricks auth login --host https://accounts.azuredatabricks.net --account-id <ACCOUNT_ID>"
      - task: "Create dev federation policy (branch-scoped, no environment key)"
        location: "Run CLI command documented in Task 1 action below"
      - task: "Create prod federation policy (environment-scoped, exactly `environment:prod`)"
        location: "Run CLI command documented in Task 1 action below"
      - task: "Add SP to workspace with 'Can Manage' role or sufficient permissions"
        location: "Databricks workspace → Admin Console → Users → Service Principals → Add SP → assign workspace role"
---

<objective>
Create all three GitHub Actions workflow files (`ci.yml`, `cd-dev.yml`, `cd-prod.yml`) implementing OIDC Workload Identity Federation authentication — zero PAT tokens. Document the required human setup steps (OIDC federation policy creation, GitHub variables, GitHub Environment) in `docs/setup.md`. Verify CI is green on push to main.

Purpose: Proves the OIDC deploy path works end-to-end before any pipeline code is written. This is the authentication baseline every subsequent phase depends on.
Output: Three committed workflow files + `docs/setup.md`. Push to `main` triggers green CI + green dev deploy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-governance-ci-cd-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-governance-ci-cd-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-governance-ci-cd-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Key patterns from 01-RESEARCH.md that executor must use verbatim -->
<!-- OIDC authentication requires EXACTLY these env vars — no substitutions -->

ci.yml required env block:
```yaml
env:
  DATABRICKS_AUTH_TYPE: github-oidc
  DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
  DATABRICKS_CLIENT_ID: ${{ vars.DATABRICKS_CLIENT_ID }}
```

Federation policy subject claim format (CASE-SENSITIVE):
- Dev (branch-scoped): `repo:<org>/dbx-mls:ref:refs/heads/main`
- Prod (environment-scoped): `repo:<org>/dbx-mls:environment:prod`

The `environment: prod` key in `cd-prod.yml` job block MUST exactly match `environment:prod`
in the federation policy subject. Any case difference = silent auth failure (cryptic 401).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three GitHub Actions workflow files</name>
  <files>
    .github/workflows/ci.yml
    .github/workflows/cd-dev.yml
    .github/workflows/cd-prod.yml
  </files>

  <read_first>
    - .planning/phases/01-infrastructure-governance-ci-cd-foundation/01-RESEARCH.md (Pattern 2: Two OIDC Federation Policies; Pattern 3: GitHub Actions OIDC Workflow Structure — copy examples verbatim)
    - .planning/phases/01-infrastructure-governance-ci-cd-foundation/01-CONTEXT.md (D-05: cd-dev.yml must auto-trigger smoke test after deploy)
    - .planning/STATE.md (OIDC subject claim case-sensitivity constraint)
  </read_first>

  <action>
Write exactly the following three workflow files. Do not deviate from these templates — the OIDC claim matching is case-sensitive and the exact structure is required.

**`.github/workflows/ci.yml`** — runs on every push to any branch:

```yaml
name: CI

on:
  push:
    branches: ["**"]

permissions:
  id-token: write   # REQUIRED for OIDC token issuance
  contents: read

jobs:
  validate-and-test:
    runs-on: ubuntu-latest
    env:
      DATABRICKS_AUTH_TYPE: github-oidc
      DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
      DATABRICKS_CLIENT_ID: ${{ vars.DATABRICKS_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-dev.txt
      - uses: databricks/setup-cli@v0.295.0
      - run: databricks bundle validate
      - run: pytest tests/unit/ --cov=src --cov-report=xml
```

**`.github/workflows/cd-dev.yml`** — deploys to dev on push to main. CRITICAL: NO `environment:` key on this job — a branch-scoped federation policy is used. Adding `environment: dev` would break OIDC auth:

```yaml
name: CD Dev

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: read

jobs:
  deploy-dev:
    runs-on: ubuntu-latest
    # NO environment: key here — uses branch-scoped federation policy
    # Adding environment: dev would break OIDC (policy subject is branch-scoped, not env-scoped)
    env:
      DATABRICKS_AUTH_TYPE: github-oidc
      DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
      DATABRICKS_CLIENT_ID: ${{ vars.DATABRICKS_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: databricks/setup-cli@v0.295.0
      - run: databricks bundle deploy --target dev
      - run: databricks bundle run smoke_test_job --target dev
```

**`.github/workflows/cd-prod.yml`** — deploys to prod on v* tag push. CLI pinned for reproducibility. `environment: prod` MUST exactly match the federation policy subject `repo:<org>/dbx-mls:environment:prod`:

```yaml
name: CD Prod

on:
  push:
    tags: ["v*"]

permissions:
  id-token: write
  contents: read

jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    environment: prod   # MUST match federation policy subject exactly — case-sensitive
    concurrency:
      group: prod-deploy
      cancel-in-progress: false
    env:
      DATABRICKS_AUTH_TYPE: github-oidc
      DATABRICKS_HOST: ${{ vars.DATABRICKS_HOST }}
      DATABRICKS_CLIENT_ID: ${{ vars.DATABRICKS_CLIENT_ID }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
      - uses: databricks/setup-cli@v0.295.0
      - run: databricks bundle deploy --target prod
```

After writing the files, commit them to git so the push to main can be observed in GitHub Actions.
  </action>

  <verify>
    <automated>grep -l "DATABRICKS_AUTH_TYPE: github-oidc" .github/workflows/ci.yml .github/workflows/cd-dev.yml .github/workflows/cd-prod.yml | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains `id-token: write`
    - `.github/workflows/ci.yml` contains `DATABRICKS_AUTH_TYPE: github-oidc`
    - `.github/workflows/ci.yml` contains `databricks bundle validate`
    - `.github/workflows/ci.yml` contains `pytest tests/unit/ --cov=src --cov-report=xml`
    - `.github/workflows/ci.yml` contains `actions/setup-python@v5`
    - `.github/workflows/ci.yml` contains `databricks/setup-cli@v0.295.0`
    - `.github/workflows/ci.yml` does NOT contain `DATABRICKS_TOKEN`
    - `.github/workflows/cd-dev.yml` contains `databricks bundle deploy --target dev`
    - `.github/workflows/cd-dev.yml` contains `databricks bundle run smoke_test_job --target dev`
    - `.github/workflows/cd-dev.yml` does NOT contain `environment:` (no environment key on the job — branch-scoped policy)
    - `.github/workflows/cd-dev.yml` does NOT contain `DATABRICKS_TOKEN`
    - `.github/workflows/cd-prod.yml` contains `environment: prod`
    - `.github/workflows/cd-prod.yml` contains `concurrency:`
    - `.github/workflows/cd-prod.yml` contains `group: prod-deploy`
    - `.github/workflows/cd-prod.yml` contains `cancel-in-progress: false`
    - `.github/workflows/cd-prod.yml` contains `databricks bundle deploy --target prod`
    - `.github/workflows/cd-prod.yml` contains `tags: ["v*"]`
    - `.github/workflows/cd-prod.yml` does NOT contain `DATABRICKS_TOKEN`
  </acceptance_criteria>

  <done>All three workflow files written and committed. No PAT token (`DATABRICKS_TOKEN`) appears in any workflow file. `cd-dev.yml` has no `environment:` key on the job. `cd-prod.yml` has `environment: prod`, concurrency group, and `cancel-in-progress: false`.</done>
</task>

<task type="auto">
  <name>Task 2: Write `docs/setup.md` — lean reference guide for repo replication</name>
  <files>
    docs/setup.md
  </files>

  <read_first>
    - .planning/phases/01-infrastructure-governance-ci-cd-foundation/01-CONTEXT.md (D-07 through D-10: lean reference, "bring your own Azure", cost estimates, four required topics)
    - .planning/phases/01-infrastructure-governance-ci-cd-foundation/01-RESEARCH.md (Pattern 2: federation policy CLI commands; Pattern 4: schema ownership bootstrap; Pattern 5: Databricks Secrets CLI)
    - .planning/STATE.md (Riot API key rotation concern — 24h expiry must be documented)
  </read_first>

  <action>
Write `docs/setup.md` as a lean reference guide covering all four required topics (per D-10). Apply "bring your own Azure" framing (per D-08). Include cost estimates (per D-09).

The doc has four sections in this order: (1) Prerequisites, (2) OIDC Federation Setup, (3) Unity Catalog Grant SQL, (4) Riot API Key Rotation, (5) Local Dev Environment. Add a cost estimate callout after Prerequisites.

**Section structure and required content:**

**Prerequisites section** must state:
- Azure subscription with Databricks workspace (Unity Catalog enabled) — reader provisions their own
- Explicit callout: "The author's clusters are private and cost-incurring. You cannot connect to them. To run this pipeline, provision your own Azure Databricks workspace and bear the Azure compute costs."

**Cost estimate callout** must include:
- Standard_F4s_v2 pricing reference
- Smoke test per run: ~$0.05–0.10
- Full Phase 1 deploy: ~$0.50–1.00
- Statement: "Total Phase 1 spend should be under $5 for a typical setup run"

**OIDC Federation Setup section** must include:
1. Step: Create Service Principal at Databricks account level
2. Step: Note the numeric SP ID (different from UUID client ID)
3. Step: Authenticate CLI to account console:
   ```bash
   databricks auth login --host https://accounts.azuredatabricks.net --account-id <ACCOUNT_ID>
   ```
4. Step: Create dev federation policy (branch-scoped):
   ```bash
   databricks account service-principal-federation-policy create <SP_NUMERIC_ID> --json '{
     "oidc_policy": {
       "issuer": "https://token.actions.githubusercontent.com",
       "audiences": ["https://github.com/<org>"],
       "subject": "repo:<org>/dbx-mls:ref:refs/heads/main"
     }
   }'
   ```
5. Step: Create prod federation policy (environment-scoped):
   ```bash
   databricks account service-principal-federation-policy create <SP_NUMERIC_ID> --json '{
     "oidc_policy": {
       "issuer": "https://token.actions.githubusercontent.com",
       "audiences": ["https://github.com/<org>"],
       "subject": "repo:<org>/dbx-mls:environment:prod"
     }
   }'
   ```
6. Warning callout: "The `environment: prod` in `cd-prod.yml` and `environment:prod` in the subject claim are CASE-SENSITIVE and must match exactly. A mismatch produces a cryptic 401."
7. Step: Add repo variables `DATABRICKS_HOST` and `DATABRICKS_CLIENT_ID` (not secrets)
8. Step: Create GitHub Environment `prod` with required reviewers

**Unity Catalog Grant SQL section** must include:
- Explanation: admin runs these once before first CI deploy so SP can create schemas
- Warning: "NEVER run `databricks bundle deploy` locally before the SP deploys via CI. The first deploy sets schema ownership. If a developer runs it, the human user becomes schema owner — extremely difficult to fix without dropping schemas."
- Full grant SQL for all three schemas:
  ```sql
  GRANT USE CATALOG   ON CATALOG lol_analytics                TO `<sp-application-id>`;
  GRANT CREATE SCHEMA ON CATALOG lol_analytics                TO `<sp-application-id>`;
  GRANT USE SCHEMA    ON SCHEMA lol_analytics.bronze          TO `<sp-application-id>`;
  GRANT CREATE TABLE  ON SCHEMA lol_analytics.bronze          TO `<sp-application-id>`;
  GRANT MODIFY        ON SCHEMA lol_analytics.bronze          TO `<sp-application-id>`;
  GRANT USE SCHEMA    ON SCHEMA lol_analytics.silver          TO `<sp-application-id>`;
  GRANT CREATE TABLE  ON SCHEMA lol_analytics.silver          TO `<sp-application-id>`;
  GRANT MODIFY        ON SCHEMA lol_analytics.silver          TO `<sp-application-id>`;
  GRANT USE SCHEMA    ON SCHEMA lol_analytics.gold            TO `<sp-application-id>`;
  GRANT CREATE TABLE  ON SCHEMA lol_analytics.gold            TO `<sp-application-id>`;
  GRANT MODIFY        ON SCHEMA lol_analytics.gold            TO `<sp-application-id>`;
  ```
- Verification query: `SHOW GRANTS ON SCHEMA lol_analytics.bronze` (must show SP app-id, not human email)

**Riot API Key Rotation section** must include:
- Get dev key from https://developer.riotgames.com (24-hour expiry)
- Store in Databricks Secrets:
  ```bash
  databricks secrets create-scope lol-pipeline
  databricks secrets put-secret lol-pipeline riot-api-key --string-value "<YOUR_KEY>"
  databricks secrets put-acl lol-pipeline "<sp-application-id>" READ
  ```
- Rotation process: re-run `put-secret` with new key value; no scope recreation needed
- Permanent key: register at https://developer.riotgames.com/app-registration for a production application key (no 24-hour expiry)

**Local Dev Environment section** must include:
- Databricks CLI install:
  ```bash
  curl -fsSL https://raw.githubusercontent.com/databricks/setup-cli/main/install.sh | sh
  databricks auth login --host <workspace-url>
  ```
- Python env:
  ```bash
  python -m venv .venv && source .venv/bin/activate
  pip install -r requirements-dev.txt
  ```
- Java 11 requirement for pytest local PySpark: `sudo apt-get install openjdk-11-jdk && export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64`
- Available make targets: `make validate`, `make test`, `make smoke`
  </action>

  <verify>
    <automated>grep -c "GRANT USE CATALOG" docs/setup.md && grep -c "databricks secrets put-secret" docs/setup.md && grep -c "environment:prod" docs/setup.md && echo "DOCS_CHECKS_PASSED"</automated>
  </verify>

  <acceptance_criteria>
    - `docs/setup.md` contains `GRANT USE CATALOG`
    - `docs/setup.md` contains `GRANT CREATE SCHEMA`
    - `docs/setup.md` contains `GRANT CREATE TABLE`
    - `docs/setup.md` contains `GRANT MODIFY`
    - `docs/setup.md` contains `lol_analytics.bronze`
    - `docs/setup.md` contains `lol_analytics.silver`
    - `docs/setup.md` contains `lol_analytics.gold`
    - `docs/setup.md` contains `SHOW GRANTS ON SCHEMA lol_analytics.bronze`
    - `docs/setup.md` contains `databricks secrets create-scope lol-pipeline`
    - `docs/setup.md` contains `databricks secrets put-secret lol-pipeline riot-api-key`
    - `docs/setup.md` contains `databricks secrets put-acl`
    - `docs/setup.md` contains `environment:prod` (federation policy subject)
    - `docs/setup.md` contains `refs/heads/main` (dev federation policy subject)
    - `docs/setup.md` contains the "private clusters" disclaimer (reader provisions their own)
    - `docs/setup.md` contains cost estimate reference (~$0.05 or similar)
    - `docs/setup.md` contains `JAVA_HOME` or `openjdk` (local dev pytest setup)
    - `docs/setup.md` contains `make validate` and `make test` and `make smoke`
  </acceptance_criteria>

  <done>`docs/setup.md` exists and covers all four required topics: OIDC federation setup (with exact CLI commands and case-sensitivity warning), UC grant SQL (all three schemas), Riot API key rotation, and local dev environment. Cost estimate and "bring your own Azure" framing are present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Three GitHub Actions workflow files implementing OIDC authentication. `docs/setup.md` with all human setup instructions.

Before this checkpoint, the human must complete the prerequisite setup steps from `docs/setup.md`:
1. Create the Service Principal (Databricks account console)
2. Create the dev federation policy (branch-scoped: `repo:<org>/dbx-mls:ref:refs/heads/main`)
3. Create the prod federation policy (environment-scoped: `repo:<org>/dbx-mls:environment:prod`)
4. Add repository variables `DATABRICKS_HOST` and `DATABRICKS_CLIENT_ID` (GitHub Settings → Variables)
5. Create GitHub Environment `prod` with required reviewer protection
6. Add SP to Databricks workspace with appropriate permissions
7. Push the committed workflows to GitHub (push to main)
  </what-built>

  <how-to-verify>
1. Go to the GitHub repo → Actions tab
2. Observe the latest push to main triggered two workflows: "CI" and "CD Dev"
3. Verify "CI" completed green (validate + pytest both passed)
4. Verify "CD Dev" completed green (deploy --target dev succeeded; smoke_test_job may fail until Plan 01-04 creates it — that is expected at this stage)
5. Go to Settings → Secrets and variables → Actions → Secrets tab
6. Confirm: ZERO secrets listed (no DATABRICKS_TOKEN, no PAT tokens)
7. Confirm: Variables tab shows DATABRICKS_HOST and DATABRICKS_CLIENT_ID
  </how-to-verify>

  <resume-signal>Type "approved" when CI + CD Dev are green, OR describe the exact error message if authentication fails (include the full GitHub Actions log excerpt).</resume-signal>
</task>

</tasks>

<verification>
Final verification after checkpoint approval:

```bash
# CI workflow validates bundle and runs pytest
grep "databricks bundle validate" .github/workflows/ci.yml
grep "pytest tests/unit/" .github/workflows/ci.yml

# CD dev deploys and runs smoke test
grep "databricks bundle deploy --target dev" .github/workflows/cd-dev.yml
grep "databricks bundle run smoke_test_job" .github/workflows/cd-dev.yml

# CD prod has all required safety constraints
grep "environment: prod" .github/workflows/cd-prod.yml
grep "cancel-in-progress: false" .github/workflows/cd-prod.yml
grep "tags:" .github/workflows/cd-prod.yml

# No PAT tokens anywhere
grep -r "DATABRICKS_TOKEN" .github/workflows/ && echo "FAIL: PAT token found" || echo "PASS: No PAT tokens"
```
</verification>

<success_criteria>
- Push to `main` triggers `ci.yml` (validate + pytest) AND `cd-dev.yml` (deploy) — both green in GitHub Actions
- Zero `DATABRICKS_TOKEN` or PAT secrets in GitHub (only repository variables)
- `cd-prod.yml` deploys only on `v*` tags with `environment: prod` and `concurrency.cancel-in-progress: false`
- `docs/setup.md` covers all four required topics with exact CLI commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-governance-ci-cd-foundation/01-02-SUMMARY.md` following the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
